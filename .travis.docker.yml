language: node_js
node_js:
  - "4"

sudo: required

services:
  - docker

before_install:
  - docker build -t mangelov/bconf .

script:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push

